# --- LLM API キー (必要なものを設定) ---
# Gemini (Google GenAI): https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=
# GPT (OpenAI): https://platform.openai.com/api-keys
OPENAI_API_KEY=
# Claude (Anthropic): https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# --- サーバー設定 ---
# サーバーホスト & ポート (main.py の uvicorn で使用)
SERVER_HOST=127.0.0.1
SERVER_PORT=8000

# --- クライアント設定 ---
# Vite 開発サーバー / Electron レンダラー用ポート
CLIENT_PORT=5173

# --- ログ設定 ---
# dev: 詳細ログ出力（ボディの中身が見える）
# prod: 安全モード（ボディの中身を隠す・デフォルト）
APP_ENV=dev

# true: 強制的にボディログを出す（APP_ENV より優先）
LOG_BODY=true

# --- Electron / DevTools / 警告制御 ---
# APP_ENV に連動しつつ、個別に上書き可能
# OPEN_DEVTOOLS=true で強制表示 / false で強制非表示（未設定なら APP_ENV に従う）
OPEN_DEVTOOLS=
# ELECTRON_WARNINGS=true で警告表示を強制、false で非表示（未設定: dev は表示, prod は非表示）
ELECTRON_WARNINGS=

# --- エージェント設定 (クライアントへ配布する接続情報の源) ---
AGENT_ID=spectra
THREAD_ID=thread

# --- セッション管理設定 ---
# 会話セッションのタイムアウト時間（秒）。デフォルト: 1200 (20分)
SESSION_TIMEOUT_SECONDS=1200

# 古いセッションを掃除する間隔（秒）。デフォルト: 300 (5分)
CLEANUP_INTERVAL_SECONDS=300
