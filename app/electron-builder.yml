appId: com.avatar-ui.app
productName: avatar-ui

directories:
  output: dist

files:
  - dist-electron/**
  - dist/renderer/**
  - package.json

asar: true

mac:
  target:
    - dmg
    - zip
  electronLanguages: [ja, en]
